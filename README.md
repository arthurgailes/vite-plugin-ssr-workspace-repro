Minimal reproduction of https://github.com/brillout/vite-plugin-ssr/issues/935

Relevant variables

- build.outDir
- ssr({prerender: true})

Navigate to packages/boilerplate-react-ts and run `pnpm build`
